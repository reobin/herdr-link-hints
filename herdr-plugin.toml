id = "herdr-link-hints"
name = "Link Hints"
version = "0.2.0"
min_herdr_version = "0.9.0"
description = "Vimium-style keyboard link hints: type a hint code to open a URL from the focused pane."
platforms = ["linux", "macos"]

[[build]]
command = ["bash", "install.sh"]

[[actions]]
id = "hints"
title = "Hint links in focused pane"
contexts = ["pane"]
command = ["bash", "hints-open.sh"]

# The pane's shape is decided in `picker --open`. HINTS_WIDTH and
# HINTS_HEIGHT override it.
[[panes]]
id = "picker"
# A title is required, and Herdr draws it on the pane border. In annotate
# mode the pane is a few cells of keyboard surface with nothing to announce,
# so this is the shortest title that still loads.
title = "·"
placement = "popup"
command = ["./picker"]
